import { itemReview } from 'cannabis-cart/helpers/item-review';
import { module, test } from 'qunit';

module('Unit | Helper | item review');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemReview([42]);
  assert.ok(result);
});
